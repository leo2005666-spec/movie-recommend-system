# 项目配置 - 电影推荐系统毕业设计

## 沟通语言

- 默认使用**中文**沟通
- 命令解释与操作说明使用中文
- 回答尽量详细，不要太简略

## 权限策略

- 默认允许 Bash 命令执行
- 默认允许读取、编辑、创建项目文件
- 仅在删除文件、危险操作、系统级修改时需要确认

## 项目概述

- 当前项目是**电影推荐系统毕业设计**
- 前端使用 **React + JavaScript**
- 后端使用 **Python / Node.js**
- UI 风格参考 **TMDB**、**NBA 官网**、**现代深色风格**

## 开发规范

- 优先保证**响应式布局**，适配移动端与桌面端
- 优先**复用已有组件**，避免重复造轮子
- 修改代码前先**分析依赖关系**，避免破坏性变更

## Git 规范

- 修改完成后**自动执行 `git add .`**
- 自动生成简洁的 **commit message**
- 对于较大修改**自动进行 git commit**，不需要每次询问用户是否提交
- 仅危险操作时再确认

## 错误处理

- 优先**自动分析和修复报错**，而非仅提示问题
- 修复时说明根因和修复思路

## 工具与 Provider

- Provider 通过 **cc-switch** 管理

## 文档输出

- 默认生成**中文** Markdown / PDF / README
