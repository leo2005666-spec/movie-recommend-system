# 操作清单 · 你需要做什么

> 根据你想达成的目标，按顺序执行对应步骤。

---

## 【重要】把 OMBd 电影全部换成 TMDB

**场景**：云数据库（Turso）里现在是 OMBd 的电影，你想换成 TMDB 的电影，并让详情页右侧栏显示 TMDB 信息。

**操作步骤**：

| 步骤 | 操作 |
|------|------|
| 1 | 停止后端服务（运行后端的窗口按 Ctrl+C） |
| 2 | 在 Render 控制台复制 `TURSO_DATABASE_URL`、`TURSO_AUTH_TOKEN` |
| 3 | 去 https://www.themoviedb.org/settings/api 申请 TMDB API Key |
| 4 | 打开 PowerShell，执行下面命令 |

**PowerShell 命令**（把 `你的xxx` 换成实际值）：

```powershell
cd e:\毕设\backend

$env:TURSO_DATABASE_URL="从Render复制的Turso地址"
$env:TURSO_AUTH_TOKEN="从Render复制的Turso令牌"
$env:TMDB_API_KEY="你的TMDB_Key"

npm run crawler:replace
```

`crawler:replace` 会：**清空**所有 OMBd 电影及相关数据 → **从 TMDB 导入**新电影（约 400+ 部，含 tmdb_id）

完成后执行：

```powershell
npm run dev
```

重启后端。刷新网页即可看到 TMDB 电影，详情页右侧栏会显示社交图标、原名、预算、票房等。

---

## 一、本地运行网站（看效果）

**当前状态**：后端已启动（端口 3001），前端已启动（端口 5173）

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 打开浏览器访问 **http://localhost:5173** | 前端页面 |
| 2 | 管理员登录：**admin** / **admin123** | 可管理影视、分类、用户等 |
| 3 | 进入「影视库」或「管理员 → 影视管理」 | 应能看到约 488 部电影 |
| 4 | 点击任意电影进入详情页 | 查看详情、右侧栏信息 |

**若影视管理仍显示空**：点击页面上的「刷新列表」按钮；若仍无数据，检查后端是否在运行。

---

## 二、让详情页右侧显示 TMDB 信息（社交图标、原名、预算、票房等）

需要满足两个条件：

| 条件 | 如何做 |
|------|--------|
| 1. 有 TMDB API Key | 去 https://www.themoviedb.org/settings/api 申请，填应用 URL、概述即可 |
| 2. 电影有 tmdb_id | 使用 TMDB 同步脚本导入的电影才有 |

**操作**：

```bash
cd backend
set TMDB_API_KEY=你的key
npm run crawler
```

运行前**先停止**后端（Ctrl+C），完成后再执行 `npm run dev` 重启。  
同步后，详情页右侧栏会显示 TMDB 数据（社交、原名、预算、票房等）。

---

## 三、线上部署（让朋友通过链接访问）

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 按 [DEPLOY.md](./DEPLOY.md) 部署到 Vercel + Render | 前端 Vercel，后端 Render |
| 2 | 在 Vercel 添加环境变量 `VITE_API_BASE` | 值为 `https://你的后端.onrender.com/api` |
| 3 | 若使用 Turso，需导入电影 | 见下方「往 Turso 导入电影」 |

**往 Turso 导入电影**（线上库为空时必做）：

- **方式 A：GitHub Actions（推荐，本地网络无法访问 TMDB 时）**
  1. 打开你的 GitHub 仓库 → **Settings** → **Secrets and variables** → **Actions**
  2. 添加 3 个 Secrets：
     - `TMDB_API_KEY` = 你的 TMDB Key
     - `TURSO_DATABASE_URL` = 从 Render 环境变量复制
     - `TURSO_AUTH_TOKEN` = 从 Render 环境变量复制
  3. 进入 **Actions** → 选择 **Sync Movies from TMDB** → **Run workflow**
  4. 勾选 **「清空旧数据仅保留 TMDB」**（替换 OMBd 时必勾）
  5. 点击 **Run workflow**，等待约 3～5 分钟
- **方式 B：本地执行**（需本地能访问 TMDB）
  - 编辑 `backend/执行-清空OMDb改用TMDB.ps1` 填写密钥，然后运行
  - 或：`npm run crawler:replace`（需先设置环境变量）

---

## 四、快速对照表

| 你想… | 对应章节 |
|-------|----------|
| 本地看网站效果 | 一 |
| 详情页显示 TMDB 右侧栏 | 二 |
| 部署到网上让别人访问 | 三 |
| 线上网站有电影数据 | 三 → 往 Turso 导入电影 |

---

更多细节见 [readme.md](./readme.md)、[访问说明.md](./访问说明.md)。
