# 网站访问说明 · 一眼看懂

> 给第一次打开这个项目的人看的说明，用最简单的话解释：网站怎么开、为什么有时打不开、用到的平台都是啥。

---

## 一、网站能直接打开吗？需要我先启动什么吗？

### 线上网站（已部署到互联网）

**不需要你启动任何程序。** 别人直接打开链接就能访问。

- **前端**（网页界面）：部署在 Vercel，**24 小时在线**，随时能打开
- **后端**（数据接口）：部署在 Render 免费版，**15 分钟没人访问会自动休眠**
- **数据库**：使用 Turso 云数据库时，**24 小时在线**

所以：
- 有人访问 → 网页秒开 ✓
- 后端休眠后，第一次有人访问 → 要等 **30～50 秒** 后端“睡醒”，页面才会正常加载
- 之后一段时间内再访问 → 又秒开 ✓

**结论**：线上网站**可以随时打开**，但长时间没人访问后，第一次打开会慢一点（等后端唤醒），这是正常现象。

---

## 二、为什么朋友的网站随时能打开，我的有时要等？

常见原因：

| 情况 | 说明 |
|------|------|
| **朋友用了付费服务** | 付费版 Render / 其他云服务不会休眠，一直在线 |
| **朋友的网站流量大** | 经常有人访问，后端一直“醒着”，不会休眠 |
| **朋友用了不同平台** | 如 Railway、Fly.io 等，免费策略不同 |
| **你的网站用 Render 免费版** | 15 分钟无访问就休眠，首次访问需冷启动 30～50 秒 |

**解决办法**（任选其一）：
1. **接受现状**：告诉访问者“首次打开可能要等半分钟”，刷新或稍等即可
2. **升级 Render**：付费版不休眠，约 $7/月起
3. **换平台**：如 Railway、Fly.io 等，部分免费额度更宽松
4. **定时保活**：用免费监控服务（如 UptimeRobot）每 10 分钟访问一次后端，减少休眠

---

## 三、Render、Vercel、Turso 都是什么？

用一句话说明：

| 平台 | 一句话解释 | 在本项目里的作用 |
|------|------------|------------------|
| **Vercel** | 托管前端网页的云平台，类似“网页寄存” | 存放你的 React 前端，用户打开网站时看到的就是它 |
| **Render** | 托管后端程序的云平台，类似“服务器托管” | 运行你的 Node.js 后端，处理登录、推荐、评论等逻辑 |
| **Turso** | 云数据库服务，类似“网上数据库” | 存放用户、电影、评分等数据，Render 重启也不会丢 |

### 更通俗的比喻

- **Vercel** = 展示厅（别人来看的页面）
- **Render** = 后台办公室（处理请求、算推荐、查数据）
- **Turso** = 档案室（存用户、电影、评分等）

用户打开网站 → 先到 Vercel 看页面 → 页面再向 Render 要数据 → Render 去 Turso 查数据 → 返回给用户。

---

## 四、本地运行（开发调试）

如果你想在**自己电脑**上运行网站，需要手动启动前后端：

```bash
# 终端 1：启动后端
cd backend
npm install
npm run init-db
npm run dev

# 终端 2：启动前端
cd frontend
npm install
npm run dev
```

然后浏览器打开：http://localhost:5173

**本地运行**和**线上访问**是两回事：本地只有你自己能访问，线上是部署到云上，别人通过链接就能打开。

---

## 五、快速对照表

| 问题 | 答案 |
|------|------|
| 别人能直接打开我的网站吗？ | 能，发链接即可 |
| 需要我先启动什么吗？ | 不需要，已部署好就 24 小时在线 |
| 为什么有时打开很慢或没反应？ | 后端休眠了，等 30～50 秒或刷新即可 |
| 为什么朋友的网站不会这样？ | 可能用了付费服务或流量更大 |
| Vercel 是什么？ | 托管前端网页的平台 |
| Render 是什么？ | 托管后端程序的平台 |
| Turso 是什么？ | 云数据库，存用户和电影数据 |

---

更多部署细节见 [DEPLOY.md](./DEPLOY.md)。
